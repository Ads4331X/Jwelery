# TODO

## Admin Dashboard Overview (Minimal)

- [x] Update `src/features/admin/components/overview/Overview.tsx` to remove hardcoded stats and placeholders.
- [x] Add a small reusable recent-activity card component under `src/features/admin/components/overview/`.
- [x] Use existing data sources only: `fetchAdminProducts()` and `fetchContacts()`.
- [x] Replace “Recent Orders” placeholder with a lightweight “Recent enquiries” list.
- [x] Keep labels concise and UI minimal.
- [x] Run `npm run build` to verify TypeScript/MUI compile.
